# タスク管理アプリ 技術設計書

- 文書版: 0.1（第一試作版）
- 対象: Windows 11 / Python 3.12.2
- 実行方式: プロジェクト専用仮想環境 + `pythonw.exe`

## 1. 採用技術

| 用途 | 採用 | 方針 |
|---|---|---|
| GUI | PySide6 Essentials 6.10.3 / Qt Widgets | Windowsデスクトップ、通知領域、複数ウィンドウ、DPI対応を1つのGUI基盤で実装し、3D・Web等の未使用モジュールは導入しない |
| DB | SQLite + SQLAlchemy 2.0.52 | 1プロセスからローカルDBを操作する |
| DB移行 | Alembic 1.19.1 | SQLite向けのbatch migrationを使用する |
| 祝日 | jpholiday 1.0.3 | 外部APIを使わず日本の祝日を判定する |
| テスト | pytest 9.1.1 + pytest-qt 4.5.0 | DBとGUIの主要操作を自動検証する |
| 整形・検査 | Ruff 0.16.4 | コード整形と基本的な静的検査を1本化する |

GUIはQt Quick/QMLではなくQt Widgetsを使用する。個人用ツールとして長期改修しやすく、Pythonだけで画面と動作を追跡しやすい構成を優先する。

## 2. アーキテクチャ

```text
画面・ウィンドウ
        ↓
アプリケーションサービス
        ↓
リポジトリ / SQLAlchemy
        ↓
SQLite（D:\TaskManager\Data\tasks.db）

Windows固有機能
  ├─ 通知アダプター
  ├─ グローバルホットキー
  └─ スタートアップ登録
```

画面からSQLを直接実行しない。タスク生成、状態変更、繰り返し生成、リマインド計算はサービス層に集約する。Windows通知などはアダプター化し、実機検証で方式が変わってもGUIとDBに影響させない。

## 3. プロジェク構成

```text
Task-Manager\
├─ src\dandori\
│  ├─ domain\          # 列挙値、業務ルール
│  ├─ infrastructure\  # DB、設定、Windows機能
│  ├─ services\        # タスク管理ユースケース
│  └─ ui\              # PySide6画面
├─ migrations\             # Alembic移行スクリプト
├─ tests\
├─ scripts\                # Windowsセットアップと起動
├─ pyproject.toml
└─ requirements.txt
```

## 4. データ保存

### 4.1 保存先の決定

1. 起動引数 `--data-dir`
2. 環境変数 `TASK_MANAGER_DATA_DIR`
3. `%LOCALAPPDATA%\TaskManager\bootstrap.json`
4. Windowsでは `D:\TaskManager\Data`
5. Windows以外の開発環境ではプロジェク直下 `.dandori-data`

Windows実運用でDドライブが利用できない場合は勝手に別ドライブへ保存せず、理由を表示して起動を中止する。

### 4.2 SQLite設定

- `PRAGMA foreign_keys=ON`
- `PRAGMA journal_mode=WAL`
- `PRAGMA synchronous=NORMAL`
- `PRAGMA busy_timeout=5000`
- 書き込みはサービス単位のトランザクションで実行する
- SQLiteのトランザクション挙動はPython 3.12で明示的に設定する

## 5. 主要テーブル

| テーブル | 内容 |
|---|---|
| `tasks` | 主タスク、状態、重要度、期限、メモ、保存期間、ゴミ箱情報 |
| `subtasks` | 1階層のサブタスク |
| `categories` | カテゴリ名と表示色 |
| `tags` / `task_tags` | タグとタスクの多対多関係 |
| `recurrence_groups` | 一括生成した繰り返しタスクの関連付け |
| `reminder_profiles` | 重要度別の標準プロファイル |
| `reminder_rules` | 開始条件、終了条件、間隔、回数制限 |
| `reminder_events` | 通知予定と実行結果 |
| `attachments` | アプリ管理領域のファイル情報 |
| `task_history` | 変更前後の値と変更日時 |
| `settings` | テーマ、勤務時間、画面幅など |

主キーにUUID文字列を使用し、添付ファイルのディレクトリ名と一致させる。

## 6. GUI構成

### 6.1 全表示

- 左: ナビゲーション
- 中央: 一覧またはカレンダー
- 右: 選択したタスクの詳細
- 一覧とカレンダーは同じタスクサービスを使用する

カレンダーは7列の月表示とする。日付を選択すると右側を当日タスク一覧へ切り替え、そのタスクを選択した後だけ詳細を表示する。

### 6.2 右端ウィンドウ

- 180px、240px、320pxの3段階で切り替え、選択幅をDBへ保存する
- 利用可能なスクリーン高さ全体を使用する
- 全幅で操作要素を1列にし、文字とメモは折り返す
- 右端タスク表示とタスク追加は別ウィンドウとする
- 右端タスクは期限切れ、今日、今後、期限なしでグループ化する
- カード選択後は同じ右端ウィンドウ内で詳細を表示する
- 右端追加から詳細入力へ、入力途中の値を引き継ぐ

### 6.3 テーマとフォント

- QSSでダーク/ライトと11種類のカラーパレットを組み合わせる
- 選択したテーマは`settings`テーブルへ保存し、起動時に適用する
- Noto Sans JP Medium / Boldをアプリに同梱する
- 画面上には固有のアプリ名や文字ロゴを表示しない

## 7. リマインド実行

1分ごとのタイマーでDB上の次回通知を確認する。スリープ復帰や異常終了後も、現在時刻と通知履歴から再計算する。長時間の個別タイマーは保持しない。

Windows通知の「完了」「10分後」は通知起動引数として受け取り、既存プロセスへローカルソケットで伝達する。通知APIの最終選定はWindows実機検証後とする。

## 8. シングルインスタンスと自動起動

- `QLocalServer` / `QLocalSocket`で二重起動を防止する
- 2回目の起動引数は既存プロセスへ送る
- Windowsログイン時はスタートアップ登録した `pythonw.exe -m dandori --tray`を実行する
- EXE化は初期運用に必須としない

## 9. 第一試作版の範囲

第一試作版で以下を実装する。

- SQLite初期化と「未分類」カテゴリ生成
- タスクの新規登録、表示、編集、完了
- タスク名、メモ、状態、重要度、期限、カテゴリ
- 一覧表示と月カレンダー表示
- カレンダーの日付選択 → 当日タスク → 詳細表示
- ダーク/ライトと11種類のカラーパレット
- 基本的な右端タスク表示とタスク追加画面
- Windowsで実行するための仮想環境作成スクリプト
- グローバルホットキー、自動起動、シングルインスタンス
- カテゴリ管理と期限時刻プルダウン
- 今日・すべて・期限切れ・完了済み表示と完了タスクの復旧
- 完了直後の取り消し操作
- 右端タスクの期限別グループと画面内詳細
- カレンダー選択日を引き継ぐタスク追加
- 小さい画面向けのスクロールと最小サイズ調整

以下は第二段階以降とする。

- Windows通知と通知ボタン
- 重要度別・個別のリマインド計算
- サブタスク、繰り返し一括生成、添付
- 一覧の一括編集と変更履歴からの復元
- 自動削除、ゴミ箱、ZIP更新用移行

## 10. 第一試作版の確認項目

1. Windows 11 / Python 3.12.2で起動できる。
2. DドライブのDBへタスクが保存され、再起動後も残る。
3. 一覧とカレンダーで同じタスクが表示される。
4. カレンダーの日付とタスクを選択できる。
5. 右端画面が180px、240px、320pxで表示され、文字や操作が欠けない。
6. ダーク/ライトの両方で文字を読める。
7. 終了後にDBファイルが破損しない。
