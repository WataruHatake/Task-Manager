# DANDORI 要件定義書

- 文書版: 1.0（構想確定版）
- 作成日: 2026年8月22日
- 対象OS: Windows 11
- 利用者: 1名（個人専用）

## 1. 目的

Windows PC上で、日々のタスクをすぐに登録し、期限・重要度・状態・リマインドを柔軟に管理できる個人用タスク管理アプリを構築する。

アプリはWindowsログイン時から常駐し、全画面を開かなくても、画面右端の小型画面からタスクの確認と追加を行えるものとする。

## 2. 基本方針

| 項目 | 方針 |
|---|---|
| 利用形態 | 個人専用 |
| アプリ名称 | DANDORI（ダンドリ） |
| 通信 | 完全ローカル。通常利用時に外部通信しない |
| 開発・実行環境 | Python 3.12.2 |
| Pythonライブラリ | pipによる追加インストールが可能。プロジェクト専用の仮想環境へ導入する |
| データベース | SQLite |
| DBライブラリ | ORM、マイグレーションなどの追加ライブラリをpipで導入可能 |
| 保存先 | `D:` ドライブ |
| 表示言語 | 日本語のみ |
| デザイン | 黒・白・ライムグリーンを基調としたプロフェッショナルなスタイル |
| フォント | Noto Sans JPを採用する。本文はMedium相当、見出しと主要操作はBold相当で表示し、細字は使用しない。フォントはアプリに同梱し、実行時に外部通信しない |
| テーマ | ダーク／ライト切り替え対応。初期値はダーク |
| バックアップ | 通常バックアップ・エクスポート機能は設けない |
| 外部連携 | Outlook、Teams、Geminiなどとは連携しない |
| 完成基準 | 利用者による試用と改善を繰り返し、利用者が満足した時点 |

## 3. システム構成

Pythonパッケージはアプリ専用の仮想環境へ導入し、Windows全体のPython環境へ直接インストールしない。採用パッケージとバージョンは依存関係ファイルで固定し、新規セットアップと更新時に同じ環境を再現できるようにする。

SQLiteは完全ローカルのデータベースとして使用する。DB操作、スキーマ管理、マイグレーションについては、Python標準機能だけに限定せず、pipで導入可能なライブラリを技術設計時に選定する。

### 3.1 配置

```text
D:\
└─ TaskManager\
   ├─ App\
   │  ├─ アプリのソースコード
   │  └─ Python仮想環境
   │
   └─ Data\
      ├─ tasks.db
      ├─ attachments\
      └─ migration_backup\
```

アプリ本体と利用者データを分離し、アプリを入れ替えても `D:\TaskManager\Data` を維持できる構成とする。正式名称が決まっても、保存パスは安定性のため変更しない。

端末固有の最小設定として、データ保存先を示す設定ファイルを以下へ保存する。

```text
%LOCALAPPDATA%\TaskManager\bootstrap.json
```

カテゴリ、タグ、テーマ、勤務時間、重要度別リマインドなどの利用者設定はSQLiteデータベース内へ保存する。

### 3.2 データ量

- タスク作成数は最大で1日3件程度を想定する。
- 年間約1,100件に加え、繰り返しタスクとサブタスクを扱えること。
- 添付ファイルの容量には固定上限を設けない。
- 1ファイルが100MBを超える場合は、保存前に警告する。
- Dドライブの空き容量が不足している場合は添付を中止し、理由を表示する。

## 4. タスク情報

### 4.1 主タスク

主タスクは以下の情報を保持する。

| 項目 | 必須 | 内容 |
|---|---:|---|
| タスク名 | 必須 | タスクの名称 |
| メモ | 任意 | プレーンテキスト |
| 状態 | 必須 | 未着手、進行中、保留、完了、取り消し |
| 重要度 | 必須 | 低、通常、高、最優先、超最優先 |
| 期限日 | 任意 | カレンダーから選択可能 |
| 期限時刻 | 任意 | 選択または直接入力可能 |
| カテゴリ | 必須 | 初期値は「未分類」。1タスクにつき1つ |
| タグ | 任意 | 1タスクに複数設定可能 |
| リマインド | 任意 | 標準プロファイルまたは個別ルール |
| 繰り返しグループ | 任意 | 一括生成したタスクの関連付け |
| 保存期間 | 任意 | 初期値は完了・取り消しから1年 |
| 作成日時 | 自動 | システムが記録 |
| 更新日時 | 自動 | システムが記録 |
| 完了・取り消し日時 | 自動 | 状態変更時に記録 |
| 削除予定日 | 自動 | ゴミ箱移動時に記録 |

期限日だけを指定し、期限時刻を指定しなかった場合は、その日の17:00を内部的な期限時刻として扱う。

期限なしのタスクも作成できること。

### 4.2 状態

状態は以下の5種類とし、後から変更可能とする。

1. 未着手
2. 進行中
3. 保留
4. 完了
5. 取り消し

保留中はリマインドを一時停止する。完了または取り消しにした場合は、以降のリマインドを停止する。

### 4.3 重要度

重要度は以下の5段階とする。

1. 低
2. 通常
3. 高
4. 最優先
5. 超最優先

各重要度には標準リマインドプロファイルを設定し、設定画面から変更可能とする。

### 4.4 カテゴリとタグ

- カテゴリは利用者が自由に作成、名称変更、色変更できること。
- 初期カテゴリは「未分類」のみとする。
- タグは自由入力とし、1タスクへ複数設定できること。
- 一覧の検索・絞り込みでカテゴリとタグを利用できること。

## 5. サブタスク

サブタスクは1階層のみとし、サブタスクの下にさらにサブタスクを作成しない。

サブタスクは以下の情報を保持する。

- サブタスク名
- 完了チェック
- 状態
- 期限日時
- 重要度
- 親タスクから継承するカテゴリ

サブタスクには独自のメモ、添付、タグ、繰り返し、リマインドを設定しない。

親タスクを完了する際に未完了のサブタスクがある場合は警告する。サブタスクの進捗は通常カードには表示せず、タスクの詳細表示内で `3/5` のように表示する。

## 6. 添付ファイル

- 画像、PDF、Word、Excel、PowerPointなどのファイルを添付できること。
- 添付時に、元ファイルへのリンクではなく、アプリ管理領域へファイルをコピーする。
- 保存先は `D:\TaskManager\Data\attachments\<タスクID>\` とする。
- 元ファイルが移動または削除されても、添付ファイルを開けること。
- 画像はアプリ内でプレビューする。
- PDF・OfficeファイルなどはWindowsの既定アプリで開く。
- タスクをゴミ箱へ移した場合は添付も一緒に移動し、復元時に戻す。
- タスクの完全削除時に添付も完全削除する。

## 7. 繰り返しタスク

### 7.1 生成方式

完了時に次回タスクを作成する方式は使用しない。

タスク作成時に以下を指定し、対象期間分の独立したタスクを一括生成する。

- 開始日
- 終了日（対象期間に含む）
- 対象曜日（複数選択可能）
- 期限時刻
- 重要度
- カテゴリ
- タグ
- リマインド設定
- 祝日にも生成するか

保存前に生成件数、最初の期限日、最後の期限日を表示し、利用者の確認後に生成する。

標準では日本の祝日を除外し、タスクごとに「祝日も生成」を選択できること。祝日判定は外部APIを使用せず、アプリへ同梱するローカルデータを使用する。

### 7.2 生成後の編集

一括生成したタスクを編集する場合は、以下から適用範囲を選択できること。

1. このタスクだけ
2. この日以降
3. グループすべて

生成済みタスクは個別に状態変更、延期、編集できること。指定した終了日より後のタスクは自動生成しない。

## 8. リマインド

### 8.1 共通ルール

- Windows標準のアプリ通知として画面右下へ表示する。
- 標準通知時間帯は09:00～17:00とする。
- 標準時間帯は設定画面で変更可能とする。
- タスクごとに時間帯を上書きできること。
- 時間帯外に該当した通知は送信せず、次の通知可能時間に再計算する。
- 保留、完了、取り消し状態では通知しない。
- 通知を閉じただけではタスク状態を変更しない。
- その通知回を閉じても、後続の設定済み通知は継続する。

### 8.2 標準プロファイル

| 重要度 | 標準リマインド |
|---|---|
| 低 | 期限時刻に1回。期限超過通知なし |
| 通常 | 1時間前、期限時刻。期限超過後は3時間ごと |
| 高 | 前日16:30、3時間前、1時間前、期限時刻。期限超過後は1時間ごと |
| 最優先 | 24時間前から3時間ごと、期限当日は1時間ごと、残り1時間は10分ごと、期限超過後は1時間ごと |
| 超最優先 | 前日または24時間前から3時間ごと、前日16:30、期限当日は1時間ごと、残り3時間は10分ごと、期限超過後は1時間ごと |

超最優先で時刻指定のない期限は、期限前日の15:00から通知を開始する。時刻指定がある期限は、期限の24時間前から通知を開始する。

前日16:30の退勤前通知は、期限前日に1回だけ表示する。

期限超過後の繰り返し通知は、プロファイルに定めた間隔で通知可能時間帯内に実行し、完了、保留、取り消しになるまで継続する。

### 8.3 カスタマイズ

各タスクは以下から選択できること。

- 重要度の標準設定を使用
- 標準設定を複製して個別カスタマイズ
- リマインドなし
- 完全に独自の通知ルールを設定

独自ルールでは以下を組み合わせられること。

- 期限の指定時間前に1回
- 指定日時に1回
- 指定期間中、一定間隔
- 期限当日になったら一定間隔
- 残り時間による間隔変更
- 指定時刻の固定通知
- 期限超過後の繰り返し
- 曜日・通知可能時間帯
- 最大通知回数

標準プロファイルを変更した場合、「標準設定を使用中」の登録済みタスクへ反映する。個別カスタマイズ済みタスクには反映しない。保存前に反映対象件数を表示する。

通常のタスク編集画面にはリマインドの概要と次回通知日時だけを表示し、複雑なルールは「詳細設定」を開いた場合だけ表示する。

### 8.4 通知操作

通常通知には以下を設ける。

- 通知本体クリック: 右端タスク表示モードを開き、対象タスクを選択
- 「完了」: 画面を開かずに完了へ変更
- 「10分後」: 期限を変更せず、リマインドを10分延期

1時間後など、その他の延期時間はアプリ内から選択する。

ロック画面ではタスク名やメモを表示せず、「期限の近いタスクがあります」のような一般的な文言だけを表示する。

同時刻に複数の通常通知がある場合は、タスクごとに個別表示する。

PCがスリープしていた場合は、復帰時に見逃したリマインドを再計算する。見逃しが複数ある場合は、件数と概要をまとめた通知を1件表示する。

アプリが異常終了した場合は、次回起動時に未通知分を再計算する。

## 9. 画面モード

### 9.1 全表示モード

管理用のメイン画面として、一覧表と右側の詳細編集欄を表示する。

初期画面は「今日のタスク」とする。

一覧には以下の列を表示する。

- タスク名
- 状態
- 重要度
- 期限
- カテゴリ
- タグ
- サブタスク完了数
- 次回通知
- 更新日時

状態、重要度、期限、カテゴリを一覧上で直接編集できること。編集内容は即時保存せず、画面下部に未保存状態を表示し、「変更を保存」で確定、「元に戻す」で破棄する。

複数タスクを選択し、状態、重要度、期限、カテゴリを一括変更できること。一括変更も保存操作で確定する。

完了済みタスクは通常一覧から除外し、「完了済み」画面から確認する。

### 9.2 カレンダー表示

- 月表示と週表示を切り替えられること。
- 月表示は一般的な7列のカレンダー形式とし、縦長のタスクカードや細長いラベルは使用しない。
- 日付セル内のタスク名は小さめの文字で約4文字を目安に短縮表示する。
- 日付を選択すると、右側にその日のタスク一覧を表示する。
- 右側のタスクを選択すると、同じ領域をタスク詳細表示に切り替える。
- タスク未選択時は詳細表示や `TASK DETAIL` などの見出しを表示しない。
- 期限日をカレンダーから選択できること。
- カレンダー上のタスクを別の日へドラッグして期限日を変更できること。
- ドラッグ時は期限時刻を維持すること。
- 完了済みタスクは薄い色で表示すること。
- 「次の平日」は月曜日～金曜日から日本の祝日を除外して計算すること。

### 9.3 タスク追加モード

グローバルショートカット `Ctrl + Alt + N` で呼び出す。

画面右端へ幅約180pxの小型画面として表示する。画面解像度、Windowsの表示倍率、利用可能領域に応じて自動調整し、以下の項目だけを表示する。

- タスク名
- 期限
- 重要度
- カテゴリ
- 追加ボタン
- 詳細設定ボタン

登録後は初期設定で自動的に閉じる。設定により、登録後も開いたままに変更できること。

### 9.4 右端タスク表示モード

グローバルショートカット `Ctrl + Alt + T` で呼び出す。

- マウスカーソルがあるモニターの右端へ表示する。
- 画面の高さいっぱいに表示する。
- 基準幅は約180pxとし、画面解像度、Windowsの表示倍率、利用可能領域に応じて自動調整する。
- 幅は160～320pxの範囲で変更でき、最後の幅を記憶する。
- 240px未満では入力項目と操作ボタンを1列で表示し、240px以上では表示領域に余裕がある項目のみ2列で表示する。
- 画面の縦横比、表示倍率、ユーザーが変更した幅にかかわらず、文字の重なり、要素のはみ出し、操作不能なボタンを発生させない。
- 呼び出してから閉じるまで表示する。
- 他のアプリより常に手前には固定しない。
- タスクカードを1列で表示する。

表示順は以下とする。

1. 期限切れ
2. 今日
3. 最優先・超最優先
4. 今後のタスクを期限順
5. 期限なし

通常カードには以下を表示する。

- タスク名
- 期限
- 重要度
- 状態
- 完了ボタン
- 延期ボタン
- 編集ボタン

カテゴリ、次回通知、サブタスク進捗は通常カードには表示せず、詳細を開いた場合だけ表示する。

### 9.5 延期

延期操作では以下を選択できること。

- 明日
- 次の平日
- 1週間後
- 日時を指定
- リマインドだけ延期

## 10. 検索・絞り込み・並び順

検索対象は以下とする。

- タスク名
- メモ
- カテゴリ
- タグ
- サブタスク名
- 添付ファイル名

以下の条件で絞り込みできること。

- 状態
- 重要度
- 期限範囲
- カテゴリ
- タグ

標準の並び順は以下とする。

1. 期限切れ
2. 今日
3. 期限が近い順
4. 期限なし

## 11. 編集・履歴

- 登録済みタスクの全項目を後から編集できること。
- 詳細編集は「保存」ボタンで確定する。
- 一覧の直接編集も「変更を保存」で確定する。
- 保存前に変更を元へ戻せること。
- 変更日時、変更項目、変更前、変更後を履歴として保存する。
- 履歴から以前の状態へ戻せること。
- 履歴はタスクの保存期間中保持し、タスクの完全削除時に削除する。

## 12. ゴミ箱と保存期間

- タスク削除時は即時完全削除せず、ゴミ箱へ移動する。
- 添付ファイル、サブタスク、履歴も一緒に移動する。
- ゴミ箱から元の状態へ復元できること。
- ゴミ箱画面に完全削除予定日を表示する。
- ゴミ箱へ移動して30日後に、確認なしで自動的に完全削除する。

未着手、進行中、保留のタスクは自動削除しない。

完了・取り消しタスクは、完了日または取り消し日から1年間保存した後、ゴミ箱へ移動する。

タスクごとに以下の保存期間を選択できること。

- 30日
- 90日
- 180日
- 1年（初期値）
- 3年
- 無期限
- 任意の日付

## 13. 常駐・起動・終了

- Windowsログイン時に自動起動する。
- 自動起動時は全表示画面を開かず、通知領域へ直接常駐する。
- Pythonのまま運用し、コンソールを表示しない `pythonw.exe` を使用する。
- 自動起動だけを理由にEXE化しない。
- 同時に複数プロセスを起動しない。
- ウィンドウの `×` では終了せず、通知領域へ格納する。
- 通知領域のメニューに「全表示」「タスク追加」「タスク表示」「完全終了」を設ける。
- 「完全終了」を選択した場合のみ常駐プロセスを終了する。

## 14. テーマ・デザイン

- 黒・白・ライムグリーンを基調とした、端正でプロフェッショナルな印象にする。
- 情報量が多い画面でも視認性を保ち、業務アプリとして落ち着いた独自デザインとする。
- ダークテーマを初期値とする。
- 設定からライトテーマへ切り替えられること。
- 詳細設定を折りたたみ、通常画面の情報量を抑えること。
- リマインドの複雑なルールを通常画面へ常時表示しないこと。
- Windowsのディスプレイ拡大率に追従し、文字や操作部品が欠けないこと。
- 狭い幅では、横並びの入力欄や操作ボタンを縦並びへ自動的に変更すること。
- 幅変更時に文字の重なり、横方向の欠け、操作不能なボタンを発生させないこと。

## 15. アプリ更新

`git pull` は使用しない。GitリポジトリのZIPファイルをブラウザから再ダウンロードし、アプリ本体を入れ替える。

更新手順は以下とする。

1. 新しいZIPファイルをダウンロード
2. 新しいアプリフォルダへ展開
3. 更新用セットアップを実行
4. `D:\TaskManager\Data` を自動検出
5. DBバージョンを確認
6. 必要に応じてDB構造を自動移行
7. 起動確認後に古いアプリを削除

DB構造を変更する前に `migration_backup` へ安全コピーを作成する。更新成功から7日後に安全コピーを自動削除する。この安全コピーは通常バックアップ機能とは別の、更新失敗時の復旧専用とする。

DBだけでなく `attachments` を含む `D:\TaskManager\Data` 全体を更新対象外として維持する。

## 16. 対象外

以下は本バージョンの対象外とする。

- 複数利用者・チーム共有
- クラウド同期
- Outlook・Teamsからの取り込み
- Gemini・生成AI連携
- CSV・Excel・PDFなどへのエクスポート
- 定期バックアップ
- スマートフォン対応
- Webアプリ化
- サブタスクの多階層化
- 完了時に次回の繰り返しタスクを自動生成する方式

## 17. セキュリティとプライバシー

- タスクデータを外部サービスへ送信しない。
- DドライブのBitLocker保護を前提とする。
- データベース独自のパスワード暗号化は初期版では行わない。
- ロック画面通知へタスク名、メモ、カテゴリなどを表示しない。
- アプリは管理者権限ではなく通常利用者権限で起動する。
- 回復キーや認証情報をアプリへ保存しない。

## 18. 技術検証事項

実装開始前にWindows 11実機で以下を検証する。

1. Python常駐アプリからWindows標準通知を表示できること。
2. 通知の「完了」「10分後」をPython側で受け取れること。
3. 通知クリックで既存プロセスの右端パネルを開けること。
4. Windowsログイン時に `pythonw.exe` で自動起動できること。
5. `Ctrl + Alt + N` と `Ctrl + Alt + T` をグローバル登録できること。
6. 端末のセキュリティ設定で通知、スタートアップ、Dドライブ書き込みが制限されていないこと。
7. スリープ復帰後に未通知分を再計算できること。
8. 複数モニターと表示倍率が異なる環境で、右端パネルを正しく配置できること。
9. SQLiteと添付ファイルを `D:\TaskManager\Data` へ安定して保存できること。
10. ZIP更新後にDB・添付・設定を引き継げること。
11. 採用するpipパッケージがPython 3.12.2およびWindows 11で動作すること。
12. DBライブラリによるスキーマ移行と、失敗時の安全な復旧が動作すること。

技術検証で実現できない項目が判明した場合は、代替案と影響範囲を提示し、利用者の承認後に要件を変更する。

## 19. 受入条件

以下を満たした状態を初期版の受入候補とする。

- Windowsログイン後、操作なしで通知領域へ常駐する。
- ショートカットからタスク追加画面と右端タスク画面を呼び出せる。
- タスク、サブタスク、カテゴリ、タグ、添付をローカル保存できる。
- 重要度別および個別のリマインドルールが動作する。
- Windows通知から完了と10分延期を実行できる。
- 全表示、月・週カレンダー、タスク追加、右端表示を切り替えられる。
- 繰り返しタスクを指定期間・曜日で一括生成できる。
- 一覧編集、一括変更、変更履歴、履歴からの復元が動作する。
- 完了タスクの保存期間とゴミ箱の30日削除が動作する。
- ダーク・ライトテーマで表示が崩れない。
- アプリ更新後もDB、添付、設定が維持される。
- 利用者による試用で致命的な不便やデータ欠損がない。

最終的な完成判断は、利用者本人の試用結果と満足度によって行う。
